# VirtuaLive 数据库基本信息

## 数据库概览

- **数据库名**: `virtuallive_dev`
- **字符集**: `utf8mb4`
- **排序规则**: `utf8mb4_unicode_ci`
- **总表数**: 39 个（32个核心表 + 7个预置模板表）
- **用户**: `virtual_live`
- **密码**: `123`
- **端口**: `3307`

---

##  数据库表详情

### 1. users - 用户表

存储平台所有用户信息（VTuber、创作者、粉丝、商户）

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| user_id | INT | NO | PRI | 用户ID（自增） |
| username | VARCHAR(50) | NO | UNI | 用户名（唯一） |
| email | VARCHAR(100) | NO | UNI | 邮箱（唯一） |
| password_hash | VARCHAR(255) | NO | | 密码哈希 |
| phone | VARCHAR(20) | YES | | 电话号码 |
| user_type | ENUM('vtuber','creator','fan','merchant') | NO | | 用户类型 |
| avatar_url | VARCHAR(500) | YES | | 头像URL |
| introduction | TEXT | YES | | 个人介绍 |
| registration_date | TIMESTAMP | NO | | 注册时间 |
| last_login | TIMESTAMP | YES | | 最后登录时间 |
| status | ENUM('active','banned','inactive') | NO | | 账户状态 |

**索引**: idx_username, idx_email, idx_user_type

---

### 2. user_wallet - 用户资金账户

存储用户的余额和收益信息

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| wallet_id | INT | NO | PRI | 钱包ID |
| user_id | INT | NO | UNI | 用户ID（外键） |
| balance | DECIMAL(12,2) | NO | | 账户余额 |
| total_earned | DECIMAL(12,2) | NO | | 总收入 |
| total_spent | DECIMAL(12,2) | NO | | 总支出 |
| updated_at | TIMESTAMP | NO | | 更新时间 |

**外键**: user_id → users(user_id)  
**索引**: idx_user_id

---

### 3. user_follows - 用户关注关系

存储用户之间的关注和屏蔽关系

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| follow_id | INT | NO | PRI | 关注ID |
| follower_id | INT | NO | | 粉丝ID（外键） |
| following_id | INT | NO | | 被关注人ID（外键） |
| follow_date | TIMESTAMP | NO | | 关注时间 |
| is_blocked | BOOLEAN | NO | | 是否屏蔽 |

**外键**: follower_id, following_id → users(user_id)  
**唯一约束**: unique_follow (follower_id, following_id)  
**索引**: idx_follower, idx_following

---

### 4. fan_badges - 粉丝勋章

存储粉丝在主播的勋章信息

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| badge_id | INT | NO | PRI | 勋章ID |
| vtuber_id | INT | NO | | 主播ID（外键） |
| fan_id | INT | NO | | 粉丝ID（外键） |
| badge_level | INT | NO | | 勋章等级 |
| badge_name | VARCHAR(100) | YES | | 勋章名称 |
| acquired_date | TIMESTAMP | NO | | 获得时间 |

**外键**: vtuber_id, fan_id → users(user_id)  
**索引**: idx_vtuber, idx_fan

---

### 5. live_rooms - 直播间

存储主播的直播间信息

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| room_id | INT | NO | PRI | 直播间ID |
| vtuber_id | INT | NO | | 主播ID（外键） |
| room_title | VARCHAR(200) | NO | | 直播间标题 |
| description | TEXT | YES | | 直播间描述 |
| category | VARCHAR(50) | YES | | 直播分类 |
| thumbnail_url | VARCHAR(500) | YES | | 缩略图URL |
| is_live | BOOLEAN | NO | | 是否正在直播 |
| stream_key | VARCHAR(255) | YES | UNI | 推流密钥 |
| rtmp_server | VARCHAR(255) | YES | | RTMP服务器 |
| created_at | TIMESTAMP | NO | | 创建时间 |

**外键**: vtuber_id → users(user_id)  
**索引**: idx_vtuber, idx_is_live

---

### 6. live_sessions - 直播会话

存储每次直播的会话数据

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| session_id | INT | NO | PRI | 会话ID |
| room_id | INT | NO | | 直播间ID（外键） |
| start_time | TIMESTAMP | NO | | 开始时间 |
| end_time | TIMESTAMP | YES | | 结束时间 |
| duration_minutes | INT | YES | | 直播时长（分钟） |
| viewer_count | INT | NO | | 观看人数 |
| peak_viewers | INT | NO | | 最高在线人数 |
| total_gifts_revenue | DECIMAL(12,2) | NO | | 礼物总收入 |

**外键**: room_id → live_rooms(room_id)  
**索引**: idx_room, idx_start_time

---

### 7. danmaku - 弹幕记录

存储直播间的弹幕内容

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| danmaku_id | INT | NO | PRI | 弹幕ID |
| session_id | INT | NO | | 会话ID（外键） |
| user_id | INT | NO | | 用户ID（外键） |
| content | TEXT | NO | | 弹幕内容 |
| color | VARCHAR(7) | NO | | 弹幕颜色 |
| created_at | TIMESTAMP | NO | | 发送时间 |
| is_deleted | BOOLEAN | NO | | 是否删除 |

**外键**: session_id → live_sessions(session_id), user_id → users(user_id)  
**索引**: idx_session, idx_user, idx_created_at

---

### 8. gift_donations - 礼物打赏

存储用户打赏礼物的记录

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| donation_id | INT | NO | PRI | 打赏ID |
| session_id | INT | NO | | 会话ID（外键） |
| sender_id | INT | NO | | 发送者ID（外键） |
| gift_type | VARCHAR(100) | NO | | 礼物类型 |
| gift_name | VARCHAR(100) | YES | | 礼物名称 |
| gift_value | DECIMAL(12,2) | NO | | 礼物价值 |
| quantity | INT | NO | | 数量 |
| total_value | DECIMAL(12,2) | NO | | 总价值 |
| donation_time | TIMESTAMP | NO | | 打赏时间 |

**外键**: session_id → live_sessions(session_id), sender_id → users(user_id)  
**索引**: idx_session, idx_sender, idx_donation_time

---

### 9. live_playbacks - 直播回放

存储直播回放视频信息

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| playback_id | INT | NO | PRI | 回放ID |
| session_id | INT | NO | | 会话ID（外键） |
| video_url | VARCHAR(500) | YES | | 视频URL |
| thumbnail_url | VARCHAR(500) | YES | | 缩略图URL |
| duration_minutes | INT | YES | | 视频时长 |
| size_mb | INT | YES | | 文件大小 |
| status | ENUM('processing','available','deleted') | NO | | 处理状态 |
| created_at | TIMESTAMP | NO | | 创建时间 |
| deleted_at | TIMESTAMP | YES | | 删除时间 |

**外键**: session_id → live_sessions(session_id)  
**索引**: idx_session, idx_status

---

### 10. view_history - 观看历史

存储用户的观看历史记录

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| view_id | INT | NO | PRI | 观看ID |
| user_id | INT | NO | | 用户ID（外键） |
| session_id | INT | NO | | 会话ID（外键） |
| watch_start_time | TIMESTAMP | NO | | 开始观看时间 |
| watch_duration_minutes | INT | YES | | 观看时长 |

**外键**: user_id → users(user_id), session_id → live_sessions(session_id)  
**索引**: idx_user_session (user_id, session_id)

---

### 11. asset_types - 虚拟资产类型

存储虚拟资产的分类

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| type_id | INT | NO | PRI | 类型ID |
| type_name | VARCHAR(100) | NO | UNI | 类型名称 |
| description | TEXT | YES | | 类型描述 |
| commission_rate | DECIMAL(5,2) | NO | | 佣金比率 |

**索引**: idx_type_name

**初始数据**:
- Live2D模型 (20% 佣金)
- 3D模型 (20% 佣金)
- 配音包 (25% 佣金)
- 特效 (20% 佣金)
- 服饰 (20% 佣金)

---

### 12. virtual_assets - 虚拟资产

存储创作者上传的虚拟资产

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| asset_id | INT | NO | PRI | 资产ID |
| creator_id | INT | NO | | 创作者ID（外键） |
| asset_type_id | INT | NO | | 资产类型ID（外键） |
| asset_name | VARCHAR(200) | NO | | 资产名称 |
| description | TEXT | YES | | 资产描述 |
| preview_image_url | VARCHAR(500) | YES | | 预览图URL |
| tags | VARCHAR(200) | YES | | 标签 |
| price | DECIMAL(12,2) | YES | | 价格 |
| free_download | BOOLEAN | NO | | 是否免费 |
| status | ENUM('draft','pending_review','approved','rejected','delisted') | NO | | 审核状态 |
| download_count | INT | NO | | 下载次数 |
| purchase_count | INT | NO | | 购买次数 |
| rating | DECIMAL(3,2) | NO | | 评分 |
| rating_count | INT | NO | | 评分人数 |
| created_at | TIMESTAMP | NO | | 创建时间 |
| updated_at | TIMESTAMP | NO | | 更新时间 |

**外键**: creator_id → users(user_id), asset_type_id → asset_types(type_id)  
**索引**: idx_creator, idx_status, idx_created_at, idx_asset_type

---

### 13. asset_versions - 资产版本管理

存储虚拟资产的版本历史

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| version_id | INT | NO | PRI | 版本ID |
| asset_id | INT | NO | | 资产ID（外键） |
| version_number | VARCHAR(50) | YES | | 版本号 |
| file_url | VARCHAR(500) | YES | | 文件URL |
| file_size_mb | INT | YES | | 文件大小 |
| update_notes | TEXT | YES | | 更新说明 |
| release_date | TIMESTAMP | NO | | 发布时间 |
| is_latest | BOOLEAN | NO | | 是否为最新版本 |

**外键**: asset_id → virtual_assets(asset_id)  
**索引**: idx_asset

---

### 14. asset_purchases - 资产购买记录

存储用户购买资产的记录

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| purchase_id | INT | NO | PRI | 购买ID |
| asset_id | INT | NO | | 资产ID（外键） |
| buyer_id | INT | NO | | 买家ID（外键） |
| price_paid | DECIMAL(12,2) | YES | | 支付价格 |
| purchase_date | TIMESTAMP | NO | | 购买时间 |
| transaction_id | VARCHAR(100) | YES | UNI | 交易ID |
| download_link | VARCHAR(500) | YES | | 下载链接 |
| is_refunded | BOOLEAN | NO | | 是否退款 |
| refund_date | TIMESTAMP | YES | | 退款时间 |

**外键**: asset_id → virtual_assets(asset_id), buyer_id → users(user_id)  
**索引**: idx_buyer, idx_asset, idx_purchase_date

---

### 15. asset_reviews - 资产评论与评分

存储用户对资产的评分和评论

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| review_id | INT | NO | PRI | 评论ID |
| asset_id | INT | NO | | 资产ID（外键） |
| reviewer_id | INT | NO | | 评论者ID（外键） |
| rating | INT | NO | | 评分（1-5） |
| comment | TEXT | YES | | 评论内容 |
| created_at | TIMESTAMP | NO | | 创建时间 |
| helpful_count | INT | NO | | 有用数 |

**外键**: asset_id → virtual_assets(asset_id), reviewer_id → users(user_id)  
**索引**: idx_asset, idx_reviewer

---

### 16. creator_revenue - 创作者收益记录

存储创作者的收益信息

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| revenue_id | INT | NO | PRI | 收益ID |
| creator_id | INT | NO | | 创作者ID（外键） |
| asset_id | INT | YES | | 资产ID（外键） |
| revenue_type | ENUM('asset_sale','commission','tip') | NO | | 收益类型 |
| amount | DECIMAL(12,2) | YES | | 金额 |
| commission_rate | DECIMAL(5,2) | YES | | 佣金比率 |
| revenue_date | TIMESTAMP | NO | | 收益日期 |
| settlement_status | ENUM('pending','settled','failed') | NO | | 结算状态 |

**外键**: creator_id → users(user_id), asset_id → virtual_assets(asset_id)  
**索引**: idx_creator, idx_revenue_date

---

### 17. community_posts - 社区投稿

存储用户在社区发布的投稿

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| post_id | INT | NO | PRI | 投稿ID |
| author_id | INT | NO | | 作者ID（外键） |
| title | VARCHAR(200) | NO | | 标题 |
| content | LONGTEXT | NO | | 内容 |
| category | VARCHAR(100) | YES | | 分类 |
| tags | VARCHAR(200) | YES | | 标签 |
| cover_image_url | VARCHAR(500) | YES | | 封面图URL |
| likes | INT | NO | | 点赞数 |
| comments_count | INT | NO | | 评论数 |
| views | INT | NO | | 浏览数 |
| is_deleted | BOOLEAN | NO | | 是否删除 |
| created_at | TIMESTAMP | NO | | 创建时间 |
| updated_at | TIMESTAMP | NO | | 更新时间 |

**外键**: author_id → users(user_id)  
**索引**: idx_author, idx_category, idx_created_at

---

### 18. comments - 评论

存储对社区投稿的评论

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| comment_id | INT | NO | PRI | 评论ID |
| post_id | INT | YES | | 投稿ID（外键） |
| user_id | INT | NO | | 用户ID（外键） |
| parent_comment_id | INT | YES | | 父评论ID（外键，用于回复） |
| content | TEXT | NO | | 评论内容 |
| likes | INT | NO | | 点赞数 |
| created_at | TIMESTAMP | NO | | 创建时间 |
| is_deleted | BOOLEAN | NO | | 是否删除 |

**外键**: post_id → community_posts(post_id), user_id → users(user_id), parent_comment_id → comments(comment_id)  
**索引**: idx_post, idx_user, idx_created_at

---

### 19. likes - 点赞

存储用户的点赞记录

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| like_id | INT | NO | PRI | 点赞ID |
| user_id | INT | NO | | 用户ID（外键） |
| content_type | ENUM('post','comment','asset') | NO | | 内容类型 |
| content_id | INT | NO | | 内容ID |
| created_at | TIMESTAMP | NO | | 创建时间 |

**外键**: user_id → users(user_id)  
**唯一约束**: unique_like (user_id, content_type, content_id)  
**索引**: idx_content (content_type, content_id)

---

### 20. favorites - 收藏

存储用户的收藏记录

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| favorite_id | INT | NO | PRI | 收藏ID |
| user_id | INT | NO | | 用户ID（外键） |
| content_type | ENUM('post','asset','vtuber') | NO | | 内容类型 |
| content_id | INT | NO | | 内容ID |
| created_at | TIMESTAMP | NO | | 创建时间 |

**外键**: user_id → users(user_id)  
**唯一约束**: unique_favorite (user_id, content_type, content_id)  
**索引**: idx_user_type (user_id, content_type)

---

### 21. discussion_topics - 话题讨论区

存储讨论话题

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| topic_id | INT | NO | PRI | 话题ID |
| title | VARCHAR(200) | NO | | 话题标题 |
| description | TEXT | YES | | 话题描述 |
| creator_id | INT | NO | | 创建者ID（外键） |
| category | VARCHAR(100) | YES | | 分类 |
| replies_count | INT | NO | | 回复数 |
| views | INT | NO | | 浏览数 |
| pinned | BOOLEAN | NO | | 是否置顶 |
| created_at | TIMESTAMP | NO | | 创建时间 |

**外键**: creator_id → users(user_id)  
**索引**: idx_category, idx_pinned, idx_created_at

---

### 22. discussion_replies - 讨论区回复

存储对讨论话题的回复

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| reply_id | INT | NO | PRI | 回复ID |
| topic_id | INT | NO | | 话题ID（外键） |
| user_id | INT | NO | | 用户ID（外键） |
| content | TEXT | NO | | 回复内容 |
| likes | INT | NO | | 点赞数 |
| created_at | TIMESTAMP | NO | | 创建时间 |

**外键**: topic_id → discussion_topics(topic_id), user_id → users(user_id)  
**索引**: idx_topic, idx_user

---

### 23. user_notifications - 消息通知

存储系统消息和通知

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| notification_id | INT | NO | PRI | 通知ID |
| user_id | INT | NO | | 用户ID（外键） |
| notification_type | VARCHAR(100) | YES | | 通知类型 |
| content | TEXT | YES | | 通知内容 |
| related_user_id | INT | YES | | 关联用户ID（外键） |
| related_content_type | VARCHAR(50) | YES | | 关联内容类型 |
| related_content_id | INT | YES | | 关联内容ID |
| is_read | BOOLEAN | NO | | 是否已读 |
| created_at | TIMESTAMP | NO | | 创建时间 |

**外键**: user_id → users(user_id), related_user_id → users(user_id)  
**索引**: idx_user_read (user_id, is_read), idx_created_at

---

### 24. private_messages - 私信

存储用户之间的私信

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| message_id | INT | NO | PRI | 消息ID |
| sender_id | INT | NO | | 发送者ID（外键） |
| receiver_id | INT | NO | | 接收者ID（外键） |
| content | TEXT | NO | | 消息内容 |
| is_read | BOOLEAN | NO | | 是否已读 |
| sent_at | TIMESTAMP | NO | | 发送时间 |
| read_at | TIMESTAMP | YES | | 阅读时间 |

**外键**: sender_id → users(user_id), receiver_id → users(user_id)  
**索引**: idx_receiver_read (receiver_id, is_read), idx_sent_at

---

### 25. merchandise - 周边商品

存储周边商品信息

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| merchandise_id | INT | NO | PRI | 商品ID |
| creator_id | INT | NO | | 创建者ID（外键） |
| product_name | VARCHAR(200) | NO | | 商品名称 |
| description | TEXT | YES | | 商品描述 |
| category | VARCHAR(100) | YES | | 商品分类 |
| price | DECIMAL(12,2) | YES | | 商品价格 |
| stock_quantity | INT | NO | | 库存数量 |
| product_images | VARCHAR(1000) | YES | | 商品图片（多个） |
| status | ENUM('draft','pending','listed','sold_out','delisted') | NO | | 商品状态 |
| created_at | TIMESTAMP | NO | | 创建时间 |

**外键**: creator_id → users(user_id)  
**索引**: idx_creator, idx_status

---

### 26. merchandise_inventory - 库存管理

存储商品库存信息

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| inventory_id | INT | NO | PRI | 库存ID |
| merchandise_id | INT | NO | UNI | 商品ID（外键） |
| quantity_available | INT | NO | | 可用数量 |
| quantity_reserved | INT | NO | | 预留数量 |
| last_stock_update | TIMESTAMP | NO | | 最后更新时间 |

**外键**: merchandise_id → merchandise(merchandise_id)

---

### 27. merchandise_sales - 周边销售记录

存储商品销售记录

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| sale_id | INT | NO | PRI | 销售ID |
| merchandise_id | INT | NO | | 商品ID（外键） |
| buyer_id | INT | NO | | 买家ID（外键） |
| quantity | INT | NO | | 数量 |
| price_paid | DECIMAL(12,2) | YES | | 支付价格 |
| shipping_address | TEXT | YES | | 收货地址 |
| shipping_status | ENUM('pending','shipped','delivered','returned') | NO | | 物流状态 |
| tracking_number | VARCHAR(100) | YES | | 快递单号 |
| sale_date | TIMESTAMP | NO | | 销售时间 |

**外键**: merchandise_id → merchandise(merchandise_id), buyer_id → users(user_id)  
**索引**: idx_buyer, idx_merchandise, idx_sale_date

---

### 28. vtuber_avatars - 虚拟主播形象

存储VTuber的虚拟形象模型信息，支持系统预置模板和自定义配置

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| avatar_id | INT | NO | PRI | 形象ID |
| vtuber_id | INT | NO | | 主播ID（外键） |
| avatar_name | VARCHAR(100) | YES | | 形象名称 |
| model_type | ENUM('live2d','3d','vroid','custom') | NO | | 模型类型 |
| template_id | INT | YES | | 预置模板ID（外键） |
| customization_data | LONGTEXT | YES | | JSON格式的定制数据 |
| model_file_url | VARCHAR(500) | YES | | 模型文件URL |
| thumbnail_url | VARCHAR(500) | YES | | 缩略图URL |
| is_primary | BOOLEAN | NO | | 是否为主形象 |
| created_at | TIMESTAMP | NO | | 创建时间 |

**外键**: vtuber_id → users(user_id), template_id → avatar_templates(template_id)  
**索引**: idx_vtuber, idx_template

---

### 29. vtuber_profile - VTuber主页信息

存储VTuber的频道信息和统计数据

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| profile_id | INT | NO | PRI | 档案ID |
| vtuber_id | INT | NO | UNI | 主播ID（外键） |
| channel_name | VARCHAR(200) | YES | | 频道名称 |
| bio | TEXT | YES | | 个人简介 |
| avatar_id | INT | YES | | 头像ID（外键） |
| follower_count | INT | NO | | 粉丝数 |
| total_live_minutes | INT | NO | | 总直播时长 |
| total_revenue | DECIMAL(12,2) | NO | | 总收入 |
| subscription_status | ENUM('free','premium') | NO | | 订阅状态 |
| verified | BOOLEAN | NO | | 是否认证 |

**外键**: vtuber_id → users(user_id), avatar_id → vtuber_avatars(avatar_id)

---

### 30. creator_verification - 创作者认证

存储创作者的认证信息

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| verification_id | INT | NO | PRI | 认证ID |
| creator_id | INT | NO | UNI | 创作者ID（外键） |
| verification_type | VARCHAR(100) | YES | | 认证类型 |
| submission_date | TIMESTAMP | NO | | 提交时间 |
| approval_date | TIMESTAMP | YES | | 审批时间 |
| status | ENUM('pending','approved','rejected') | NO | | 认证状态 |

**外键**: creator_id → users(user_id)

---

### 31. user_activity_log - 用户行为日志

存储用户活动的审计日志

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| log_id | INT | NO | PRI | 日志ID |
| user_id | INT | NO | | 用户ID（外键） |
| action_type | VARCHAR(100) | YES | | 操作类型 |
| action_detail | TEXT | YES | | 操作详情 |
| ip_address | VARCHAR(45) | YES | | IP地址 |
| user_agent | TEXT | YES | | 用户代理 |
| created_at | TIMESTAMP | NO | | 创建时间 |

**外键**: user_id → users(user_id)  
**索引**: idx_user_date (user_id, created_at)

---

### 32. content_moderation - 内容审核记录

存储内容举报和审核记录

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| moderation_id | INT | NO | PRI | 审核ID |
| content_type | VARCHAR(100) | YES | | 内容类型 |
| content_id | INT | YES | | 内容ID |
| content_owner_id | INT | YES | | 内容所有者ID（外键） |
| reported_by_id | INT | YES | | 举报者ID（外键） |
| reason | TEXT | YES | | 举报原因 |
| status | ENUM('pending','approved','rejected') | NO | | 审核状态 |
| moderator_id | INT | YES | | 审核员ID（外键） |
| decision_reason | TEXT | YES | | 审核决定原因 |
| created_at | TIMESTAMP | NO | | 创建时间 |
| decided_at | TIMESTAMP | YES | | 审核时间 |

**外键**: content_owner_id → users(user_id), reported_by_id → users(user_id), moderator_id → users(user_id)  
**索引**: idx_status, idx_created_at

---

## 🎨 虚拟形象预置模板系统表

### 33. avatar_templates - 虚拟形象模板主表

存储系统预置的虚拟形象模板，包括Q版小人、轮廓化形象等

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| template_id | INT | NO | PRI | 模板ID |
| template_name | VARCHAR(200) | NO | | 模板名称 |
| template_category | ENUM('cute_chibi','outline_avatar','expression_system') | NO | | 模板分类 |
| description | TEXT | YES | | 模板描述 |
| template_type | ENUM('free','basic','premium') | NO | | 模板类型 |
| preview_image_url | VARCHAR(500) | YES | | 预览图URL |
| thumbnail_url | VARCHAR(500) | YES | | 缩略图URL |
| model_data_url | VARCHAR(500) | YES | | 模型数据URL |
| is_default | BOOLEAN | NO | | 是否为默认模板 |
| is_active | BOOLEAN | NO | | 是否激活 |
| download_count | INT | NO | | 下载次数 |
| usage_count | INT | NO | | 使用次数 |
| created_at | TIMESTAMP | NO | | 创建时间 |
| updated_at | TIMESTAMP | NO | | 更新时间 |

**索引**: idx_category, idx_type, idx_is_active, idx_name, idx_created_at

---

### 34. cute_chibi_templates - Q版小人模板

存储Q版小人（可爱卡通风格）形象模板的详细配置

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| chibi_id | INT | NO | PRI | Q版模板ID |
| template_id | INT | NO | UNI | 模板ID（外键） |
| body_style | VARCHAR(100) | YES | | 身体风格 |
| head_shape | VARCHAR(100) | YES | | 头部形状 |
| eye_style | VARCHAR(100) | YES | | 眼睛风格 |
| mouth_style | VARCHAR(100) | YES | | 嘴部风格 |
| hair_template | VARCHAR(100) | YES | | 发型模板 |
| color_preset_count | INT | NO | | 配色预设数量 |
| animation_enabled | BOOLEAN | NO | | 是否启用动画 |
| customizable_parts | LONGTEXT | YES | | JSON格式：可定制部件列表 |

**外键**: template_id → avatar_templates(template_id)  
**索引**: idx_template

---

### 35. outline_avatar_templates - 轮廓化虚拟形象模板

存储轮廓化形象（Xbox Avatar/Mii风格）的模板配置

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| outline_id | INT | NO | PRI | 轮廓形象ID |
| template_id | INT | NO | UNI | 模板ID（外键） |
| base_shape | VARCHAR(100) | YES | | 基础形状 |
| body_proportions | VARCHAR(100) | YES | | 身体比例 |
| facial_features | VARCHAR(100) | YES | | 面部特征 |
| customizable_elements | LONGTEXT | YES | | JSON格式：可定制元素列表 |
| default_colors | LONGTEXT | YES | | JSON格式：默认色彩方案 |
| supports_animation | BOOLEAN | NO | | 是否支持动画 |
| max_customization_level | INT | NO | | 最大定制级别 |

**外键**: template_id → avatar_templates(template_id)  
**索引**: idx_template

---

### 36. expression_system_templates - 头像表情系统模板

存储头像表情系统的模板配置（眨眼、点头、鼓掌等）

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| expression_id | INT | NO | PRI | 表情系统ID |
| template_id | INT | NO | UNI | 模板ID（外键） |
| base_model_url | VARCHAR(500) | YES | | 基础模型URL |
| included_expressions | LONGTEXT | YES | | JSON格式：包含的表情列表 |
| expression_count | INT | NO | | 表情总数 |
| supports_custom_expressions | BOOLEAN | NO | | 是否支持自定义表情 |
| animation_format | VARCHAR(50) | YES | | 动画格式（GIF/WebP/MP4等） |

**外键**: template_id → avatar_templates(template_id)  
**索引**: idx_template

---

### 37. expression_library - 表情库

存储具体的表情动画定义（眨眼、点头、鼓掌等五种类型）

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| expression_lib_id | INT | NO | PRI | 表情库ID |
| expression_template_id | INT | NO | | 表情系统ID（外键） |
| expression_name | VARCHAR(100) | NO | | 表情名称 |
| expression_code | VARCHAR(50) | NO | UNI | 表情代码（blink/nod/clap等） |
| expression_type | ENUM('eye','face','body','hand','full_body') | NO | | 表情类型 |
| animation_file_url | VARCHAR(500) | YES | | 动画文件URL |
| animation_duration_ms | INT | YES | | 动画时长（毫秒） |
| preview_image_url | VARCHAR(500) | YES | | 预览图URL |
| is_loopable | BOOLEAN | NO | | 是否可循环 |
| created_at | TIMESTAMP | NO | | 创建时间 |

**外键**: expression_template_id → expression_system_templates(expression_id)  
**索引**: idx_template, idx_code

**表情类型说明**:
- `eye` - 眼睛表情：眨眼、眨左眼、眨右眼等
- `face` - 面部表情：微笑、惊讶、悲伤等
- `body` - 身体动作：点头、摇头、耸肩等
- `hand` - 手部动作：挥手、鼓掌、点赞等
- `full_body` - 全身动作：跳舞、转身、滑稽等

---

### 38. user_custom_avatars - 用户自定义形象

存储用户基于预置模板创建的自定义形象

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| custom_avatar_id | INT | NO | PRI | 自定义形象ID |
| user_id | INT | NO | | 用户ID（外键） |
| template_id | INT | NO | | 模板ID（外键） |
| avatar_name | VARCHAR(200) | YES | | 形象名称 |
| customization_data | LONGTEXT | YES | | JSON格式：用户的定制参数 |
| preview_image_url | VARCHAR(500) | YES | | 预览图URL |
| is_primary | BOOLEAN | NO | | 是否为主形象 |
| usage_count | INT | NO | | 使用次数 |
| created_at | TIMESTAMP | NO | | 创建时间 |
| updated_at | TIMESTAMP | NO | | 更新时间 |

**外键**: user_id → users(user_id), template_id → avatar_templates(template_id)  
**索引**: idx_user, idx_template, idx_is_primary

---

### 39. user_expression_usage - 用户表情操作记录

存储用户使用表情的历史记录和统计

| 字段名 | 数据类型 | 是否为空 | 主键 | 说明 |
|--------|---------|---------|------|------|
| usage_id | INT | NO | PRI | 使用记录ID |
| user_id | INT | NO | | 用户ID（外键） |
| expression_lib_id | INT | NO | | 表情库ID（外键） |
| used_count | INT | NO | | 使用次数 |
| last_used_at | TIMESTAMP | NO | | 最后使用时间 |

**外键**: user_id → users(user_id), expression_lib_id → expression_library(expression_lib_id)  
**唯一约束**: unique_user_expression (user_id, expression_lib_id)  
**索引**: idx_user, idx_last_used

---

##  数据库统计

| 指标 | 值 |
|------|-----|
| **总表数** | 39 |
| **核心表数** | 32 |
| **预置模板表数** | 7 |
| **用户相关表** | 8 个 |
| **直播相关表** | 5 个 |
| **资产交易表** | 6 个 |
| **社区互动表** | 6 个 |
| **商城交易表** | 3 个 |
| **系统表** | 4 个 |
| **虚拟形象模板表** | 7 个 |

---

##  关键约束

### 唯一约束
- `users.username` - 用户名唯一
- `users.email` - 邮箱唯一
- `user_wallet.user_id` - 每个用户只有一个钱包
- `user_follows` - 每个用户对另一用户只能关注一次
- `live_rooms.stream_key` - 推流密钥唯一
- `asset_purchases.transaction_id` - 交易ID唯一
- `asset_types.type_name` - 资产类型名称唯一
- `likes` - 用户对内容的点赞唯一
- `favorites` - 用户对内容的收藏唯一
- `merchandise_inventory.merchandise_id` - 每个商品一个库存记录
- `vtuber_profile.vtuber_id` - 每个主播一个档案
- `creator_verification.creator_id` - 每个创作者一个认证记录

### 级联删除
- 删除用户时，会级联删除其所有相关数据
- 删除表的主记录时，会级联删除其子表记录

---

##  核心业务流程涉及表

### 直播业务流程
```
users → live_rooms → live_sessions → danmaku
                         ↓
                    gift_donations
                         ↓
                   live_playbacks
```

### 资产交易流程
```
users (creator_id) → virtual_assets → asset_versions
                           ↓
                      asset_purchases
                           ↓
                        asset_reviews
                           ↓
                      creator_revenue
```

### 社区互动流程
```
users → community_posts → comments → likes
         ↓                            
    favorites
         ↓
   user_notifications
```

### 虚拟形象模板系统流程
```
avatar_templates (系统预置模板)
    ├─ cute_chibi_templates (Q版小人)
    ├─ outline_avatar_templates (轮廓化形象)
    └─ expression_system_templates (表情系统)
           ↓
    expression_library (表情库：眨眼/点头/鼓掌等)
           ↓
users → user_custom_avatars (基于模板创建自定义形象)
         ↓
    user_expression_usage (表情使用记录)
         ↓
vtuber_avatars (VTuber最终使用的形象，可关联模板)
```

---

##  备注

- 所有时间戳使用 `TIMESTAMP` 类型，默认当前时间
- 字符集统一为 `utf8mb4`，支持表情符号
- 所有表都使用 `InnoDB` 引擎，支持事务和级联操作
- 重要表都创建了相应的索引以提高查询性能
- 部分表包含软删除字段（`is_deleted`）用于数据保留
- `LONGTEXT` 字段用于存储 JSON 格式数据（定制参数、表情列表、色彩方案等）
- 虚拟形象模板系统支持三种预置风格：Q版小人、轮廓化形象（Mii/Xbox风格）、表情系统
- 表情系统包含5种表情类型：眼部、面部、身体、手部、全身动作

