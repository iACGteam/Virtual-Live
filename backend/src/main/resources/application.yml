server:
  port: 8081

spring:
  application:
    name: virtuallive-backend
    
  datasource:
    url: jdbc:mysql://localhost:3307/virtuallive_dev?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: virtual_live
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false
    
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
  
  # 禁用 Flyway
  flyway:
    enabled: false

logging:
  level:
    root: INFO
    com.virtuallive: DEBUG
